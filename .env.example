# Database - Neon Postgres (from Vercel)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Storage - Vercel Blob (from Vercel)
BLOB_READ_WRITE_TOKEN=

# PayloadCMS Secrets
# ⚠️ IMPORTANT: Use `openssl rand -hex 32` to generate these (NOT -base64)
# Or run: ./scripts/generate-secrets.sh
# These must NOT contain whitespace or newlines!
PAYLOAD_SECRET=
CRON_SECRET=
PREVIEW_SECRET=

# Optional: Next.js
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000
